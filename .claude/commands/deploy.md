Run the following git commands in sequence:

1. Stage all changes: `git add .`
2. Create a commit with an appropriate message summarizing the staged changes
3. Push to remote: `git push origin main`

If there are no changes to commit, inform the user and skip the remaining steps.
